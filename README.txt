you need use mysql
first:excute ColumnTypes.sql
second:modify config.properties file mysql configuration
third:modify MysqlDBHelper.java's main function,productXML("ColumnTypes",true);
      "ColumnTypes" is table name,"true" the object attribute is "userName" ,"false" is "username"
haha... it is very difficulty understand what i say,cause my english is bad!!!
